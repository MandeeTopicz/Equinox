# Copy this file to .env and fill in real values. Never commit .env.

# OpenAI API key — required for equivalence matching (text-embedding-3-small)
OPENAI_API_KEY=your-openai-api-key-here

# Kalshi's read-only market data endpoint requires no authentication, so no
# Kalshi key is needed here. See docs/DECISIONS.md for why.
